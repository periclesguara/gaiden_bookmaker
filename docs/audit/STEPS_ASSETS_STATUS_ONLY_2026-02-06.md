# Steps Page: Assets + Status Only (2026-02-06)

Summary
- `edition_steps.html` is now a read-only Production Console for Assets & Status.
- All pipeline execution moved to Runner Matrix.
- Steps page keeps only asset uploads (images/cover) and status view.

Navigation
- Runner Matrix: `/pipeline/runner/`
- Frontmatter: `/editorial/frontmatter/<book_code>/<language>/`
- Global Dashboard: `/pipeline/dashboard/`
