[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gaiden-bookmaker"
version = "0.1.0"
description = "Gaiden BookMaker"
requires-python = ">=3.10"
dependencies = [
  "Django>=4.2,<6",
]

[tool.setuptools]
packages = ["gaiden"]
